# Next-Gen Assets

Use this folder for assets owned by the rebuild. Shared brand files should live in `../shared-assets/` at the workspace root and be copied into this app only when a release package requires it.
